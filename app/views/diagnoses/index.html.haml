%h1 Listing diagnoses

%table
  %thead
    %tr
      %th Diagnostico y tratamiento
      %th Patient
      %th
      %th
      %th

  %tbody
    - @diagnoses.each do |diagnosis|
      %tr
        %td= diagnosis.diagnostico_y_tratamiento
        %td= diagnosis.patient
        %td= link_to 'Show', diagnosis
        %td= link_to 'Edit', edit_diagnosis_path(diagnosis)
        %td= link_to 'Destroy', diagnosis, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Diagnosis', new_diagnosis_path
